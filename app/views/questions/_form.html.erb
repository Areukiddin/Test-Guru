<h1> <%= question_form_header(@question) %> </h1>
<%= form_with model: [@test, @question], local: true do |form| %>
  <%= render 'shared/errors', resource: @question %>

  <div>
    <%= form.label :question %>
    <%= form.text_field :body, id: :body %>
  </div>
  <br/>
  <div>
    <%= form.submit %>
  </div>

<% end %>
