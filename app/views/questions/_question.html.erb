<li>
  <p> <%= link_to question.body, question %></p>
  <div class="buttons-row">
  <p class="button-green"><%= link_to 'Edit', edit_question_path(question) %></p>
  <p class="button-red"><%= link_to 'Delete',
              question_path(question),
              method: :delete,
              data: { confirm: 'Are you sure to delete question?' } %>
  </p>
  </div>

</li>
<br/>
